# ==========================================================================
# KAVARI · Optimización de carga (.htaccess para hosting Apache)
# ==========================================================================
# ARCHIVO NUEVO · SOLO AGREGA configuración; no elimina ni modifica nada.
# Aplica SOLO si el sitio se publica en un servidor Apache (cPanel,
# Hostinger, etc.). Si el hosting es GitHub Pages/Netlify, se ignora.
#
# Qué hace:
#   1) COMPRIME el contenido (gzip/brotli) -> menos datos transferidos.
#   2) GUARDA en caché del navegador los archivos estáticos -> visitas
#      posteriores cargan casi instantáneo.
# ==========================================================================

# --- Compresión gzip (mod_deflate) ---
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/css text/xml
    AddOutputFilterByType DEFLATE application/javascript application/json
    AddOutputFilterByType DEFLATE application/x-javascript text/javascript
    AddOutputFilterByType DEFLATE image/svg+xml application/xml
    AddOutputFilterByType DEFLATE application/vnd.ms-fontobject
    AddOutputFilterByType DEFLATE application/x-font-ttf font/opentype

    # No comprimir contenido que ya viene comprimido (imágenes, video).
    SetEnvIfNoCase Request_URI \.(?:gif|jpe?g|png|webp|avif|mp4|webm|ico)$ no-gzip dont-vary
</IfModule>

# --- Caché del navegador (mod_expires) ---
<IfModule mod_expires.c>
    ExpiresActive On

    # Archivos que cambian poco: un mes de caché.
    ExpiresByType image/gif   "access plus 1 month"
    ExpiresByType image/jpeg  "access plus 1 month"
    ExpiresByType image/png   "access plus 1 month"
    ExpiresByType image/webp  "access plus 1 month"
    ExpiresByType image/avif  "access plus 1 month"
    ExpiresByType image/svg+xml "access plus 1 month"
    ExpiresByType image/x-icon  "access plus 1 month"
    ExpiresByType font/woff2  "access plus 1 month"
    ExpiresByType font/woff   "access plus 1 month"

    # JS y CSS (se versionan con ?v=3, ?v=4 ...): una semana.
    ExpiresByType text/css                 "access plus 1 week"
    ExpiresByType application/javascript   "access plus 1 week"
    ExpiresByType text/javascript          "access plus 1 week"

    # HTML y JSON: caché corta para que los cambios aparezcan pronto.
    ExpiresByType text/html                "access plus 1 hour"
    ExpiresByType application/json         "access plus 1 hour"

    # El video de fondo (10 MB) se cachea para no repetir la descarga.
    ExpiresByType video/mp4                "access plus 1 month"
</IfModule>

# --- Encabezados de control de caché (mod_headers) ---
<IfModule mod_headers.c>
    # Un año para imágenes, fuentes y media (archivos inmutables).
    <FilesMatch "\.(gif|jpe?g|png|webp|avif|svg|ico|woff2?|mp4|webm)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>

    # Una semana para JS/CSS (se versionan con ?v=...).
    <FilesMatch "\.(css|js)$">
        Header set Cache-Control "public, max-age=604800"
    </FilesMatch>

    # Caché corta para HTML y JSON.
    <FilesMatch "\.(html|json)$">
        Header set Cache-Control "public, max-age=3600"
    </FilesMatch>
</IfModule>

# --- Seguridad: no permitir listar archivos del servidor ---
Options -Indexes